import { createSelector } from 'reselect';

const selectRooms = (state) => state.get('rooms');

// const makeSelectFilter = () => createSelector(
//   selectRooms,
//   (selectRooms) => chatState.get('test')
// );

export {
};
